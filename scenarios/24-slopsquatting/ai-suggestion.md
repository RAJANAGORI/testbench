# Copilot-shaped snippet (fictional)

Do not paste live model output into this repo. This is a made-up helper that looks like
what an LLM invents when you ask for "async utils plus stripe react bindings."

```bash
npm install python-asyncio-utils
npm install @stripe/react-v3
```

Neither name is in `infrastructure/catalog-fixture.json`. Scenario 01 is a human typo
(`request-lib`). This lab is a name that was never real.

# Auto-generated local Docker lab — 22-litellm-pypi-compromise (Python)
# Floci via host.docker.internal:4566
name: scas-22-litellm-pypi-compromise

services:
  mock-c2:
    image: python:3.12-alpine
    container_name: scas-22-litellm-pypi-compromise-mock-c2
    working_dir: /scenarios/22-litellm-pypi-compromise/infrastructure
    volumes:
      - ./:/scenarios/22-litellm-pypi-compromise
      - ../_shared:/scenarios/_shared:ro
      - ../../detection-tools:/detection-tools:ro
    environment:
      SCAS_ES_URL: "${SCAS_ES_URL:-http://host.docker.internal:9200}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: ["python", "mock_server.py"]
    ports:
      - "3022:3022"
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:3022/captured-data')",
        ]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 8s

  victim:
    build:
      context: ..
      dockerfile: 22-litellm-pypi-compromise/Dockerfile
    container_name: scas-22-litellm-pypi-compromise-victim
    network_mode: "service:mock-c2"
    environment:
      TESTBENCH_MODE: enabled
      SCAS_FLOCI_ENABLED: "${SCAS_FLOCI_ENABLED:-1}"
      SCAS_FLOCI_ENDPOINT: "${SCAS_FLOCI_ENDPOINT:-http://host.docker.internal:4566}"
      AWS_ENDPOINT_URL: "${AWS_ENDPOINT_URL:-http://host.docker.internal:4566}"
      AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID:-test}"
      AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY:-test}"
      AWS_DEFAULT_REGION: "${AWS_DEFAULT_REGION:-us-east-1}"
      AWS_EC2_METADATA_DISABLED: "true"
      SCAS_ES_URL: "${SCAS_ES_URL:-http://host.docker.internal:9200}"
    volumes:
      - ../../detection-tools:/lab/detection-tools:ro
      - ../../scripts/floci:/lab/scripts/floci:ro
    depends_on:
      mock-c2:
        condition: service_healthy
    tty: true
    stdin_open: true
    command: ["sleep", "infinity"]
